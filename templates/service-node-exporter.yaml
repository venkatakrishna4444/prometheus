apiVersion: v1
kind: Service
metadata:
  name: {{ include "centralized-monitoring.fullname" . }}-node-exporter
  labels:
    {{- include "centralized-monitoring.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9100
      targetPort: 9100
      protocol: TCP
      name: http
  selector:
    {{- include "centralized-monitoring.selectorLabels" . | nindent 4 }}

